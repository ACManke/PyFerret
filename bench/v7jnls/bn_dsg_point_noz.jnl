! bn_dsg_point_noz.jnl
! 3/2024  See discussion in PyFerret issue 107:
!         DSG trajectory files need not have depth. The same has 
!         been implemented for point data. 

use dsg_point_data.nc
show data
list sst
plot/thick sst
show symbol labnum_z, lab($labnum_z)

use dsg_point_noz.nc
show data
list sst
plot/thick sst
! These symbols are undefined as there are no labels in the z direction
show symbol labnum_z, lab($labnum_z)
